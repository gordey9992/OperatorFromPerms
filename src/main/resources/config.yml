# Настройки плагина OperatorFromPerms
# Файл расположение: plugins/OperatorFromPerms/config.yml

# Основные настройки
settings:
  # Разрешение, которое дает права оператора
  оператор-разрешение: "operatorfromperms.all"
  
  # Автоматически проверять права при входе игрока
  проверка-при-входе: true
  
  # Автоматически проверять права при изменении разрешений LuckPerms
  проверка-при-изменении-прав: true
  
  # Задержка перед проверкой прав при входе (в тиках, 20 тиков = 1 секунда)
  задержка-проверки-тики: 20
  
  # Логировать выдачу/забор прав оператора в консоль
  логировать-в-консоль: true
  
  # Безопасный режим - не давать права оператора игрокам с определенными именами
  безопасный-режим: true
  заблокированные-имена:
    - "Notch"
    - "Herobrine"
    - "Admin"
    - "Owner"

# Настройки обновления прав
настройки-обновления:
  # Обновлять права оператора при смене мира
  обновлять-при-смене-мира: true
  
  # Обновлять права оператора при получении/потере группы
  обновлять-при-смене-группы: true
  
  # Принудительно обновлять права всех онлайн-игроков при перезагрузке плагина
  обновлять-всех-при-перезагрузке: true

авторизация:
  # Минимальная длина пароля
  минимальная-длина-пароля: 6
  # Максимальная длина пароля  
  максимальная-длина-пароля: 20
  # Хэшировать пароли в базе
  хэшировать-пароли: true
  # Время сессии "запомнить меня" в часах
  время-сессии: 24
  # Проверять IP адрес при "запомнить меня"
  проверять-ip: true
  # Сообщения при авторизации
  сообщения:
    регистрация-успех: "&a🎉 Регистрация успешна! Добро пожаловать в кошару 3000!"
    логин-успех: "&a✅ Вход выполнен! Приятной игры!"
    уже-зарегистрирован: "&c❌ Вы уже зарегистрированы! Используйте /логин"
    не-зарегистрирован: "&c❌ Вы не зарегистрированы! Используйте /регистрация"
    неправильный-пароль: "&c❌ Неправильный пароль!"
    пароли-не-совпадают: "&c❌ Пароли не совпадают!"
    короткий-пароль: "&c❌ Пароль слишком короткий! Минимум {мин} символов"
    длинный-пароль: "&c❌ Пароль слишком длинный! Максимум {макс} символов"
    запомнено-успех: "&b🔒 Вы запомнены на 24 часа! При следующем входе пароль не потребуется."
    сессия-истекла: "&e⚠️ Ваша сессия истекла. Пожалуйста, войдите снова."
    ip-изменился: "&e⚠️ Обнаружен новый IP. Требуется вход."

  # Экспериментальные функции (использовать на свой страх и риск)
экспериментальные:
  # Синхронизировать права оператора с разрешением (опасно!)
  синхронизация-оп-с-разрешением: false
  
  # Автоматически исправлять расхождения между разрешением и правами оператора
  авто-исправление-расхождений: true
